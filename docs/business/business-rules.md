# Business Rules

- A project progress percentage is calculated from parsed PRD checkbox tasks.
- Completed tasks use `[x]`; pending tasks use `[ ]`.
- Tasks inside fenced code blocks are examples and must not count toward progress.
- Out-of-scope ideas should not be checkbox tasks until they become active work.
