# Troubleshooting

## Pytest Permission Errors on Windows

Use a unique temp directory inside the workspace as documented in `TESTING.md`.

## GitHub PRD Fetch Fails

Check repository visibility, branch name, PRD path, and `GITHUB_TOKEN`.
